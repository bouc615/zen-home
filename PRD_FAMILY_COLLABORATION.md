# ZenHome 极简家庭版 - 产品需求文档 (PRD) v1.2 (完善交互版)

## 1. 核心理念：隐形家庭 (Implicit Household)

- **极简逻辑**：一人一号一家庭。用户注册即拥有一个默认的、以自己 ID 命名的家庭空间。
- **共享本质**：以某一个人的家庭空间为主体，其他人接入这个空间，从而实现共享。

---

## 2. 核心交互流程详解

### 2.1 初始状态 (各自独立)

- **用户 A**：拥有家庭空间 `Space_A`。当前冰箱里有物品：🍎 苹果。
- **用户 B**：拥有家庭空间 `Space_B`。当前冰箱里有物品：🍌 香蕉。
- 此时两人各玩各的，互不可见。

### 2.2 邀请与加入 (关键权衡)

**场景**：A 邀请 B 加入。

**步骤 1：A 发起**

- A 在设置页点击“邀请家人”，生成邀请码/卡片。

**步骤 2：B 确认 (关键交互)**

- B 扫码后，系统检测到 B 当前处于独立模式且有数据（香蕉）。
- **⚠️ 必须弹窗警告**：
  > **确认加入 [A 的家] 吗？**
  > 加入后，你将与 A 共同管理同一个冰箱。
  > ⚠️ **注意**：你当前记录的物品（香蕉等）将被**封存隐藏**，界面将只显示 A 家的物品（苹果）。
- **选项**：
  1. **放弃并保留当前数据** (点击取消)
  2. **确认加入并切换** (重点)
  3. _(进阶预留)_ **合并数据** (将香蕉搬进 A 家，技术复杂度高，MVP 阶段暂不支持)

### 2.3 加入后的状态

- **B 的界面**：
  - 刷新后，B 的列表里只看到 **🍎 苹果**。
  - B 的“设置”顶部显示：正在协作管理 **[A 的家]**。
- **A 的界面**：
  - 内容不变，但设置里成员列表多了一个人：B。

### 2.4 B 原来的家庭还在吗？ (数据安全)

- **还在**。
- `Space_B` 以及里面的“🍌 香蕉”并没有被删除，只是在数据库里静静躺着。
- B 当前的 `household_id` 指向了 `Space_A`，所以查询不到 `Space_B` 的数据。

### 2.5 退出与找回

- **场景**：B 决定退出共享，自己单干。
- **操作**：B 在设置页点击“退出家庭”。
- **结果**：
  - B 的 `household_id` 重新指回自己的 ID (`user_id_B`)。
  - B 的界面瞬间刷新，**🍎 苹果**消失，失散已久的 **🍌 香蕉** 又回来了。
  - A 的界面里，B 消失，但 A 家的数据（苹果）不受任何影响。

---

## 3. 数据库模型微调

仅需在 `users` 表增加一个字段。

| 字段           | 类型 | 逻辑说明                               |
| :------------- | :--- | :------------------------------------- |
| `household_id` | UUID | 指针。决定了用户当前看的是谁家的数据。 |

- **默认状态**：`household_id` = `user_id` (自己看自己)
- **共享状态**：B 的 `household_id` = A 的 `household_id` (B 看 A 的)

## 4. 总结

这种设计极大地简化了代码逻辑（不需要数据迁移和合并），同时也保证了用户数据的安全性（退步随时可找回原数据）。
唯一的缺点是 B 在加入时会有“数据断层感”，但对于绝大多数“以一方为主”的家庭场景（如老公加入老婆早已精心维护的库），这是最自然、摩擦最小的方案。
